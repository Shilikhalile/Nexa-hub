document.addEventListener('DOMContentLoaded',()=>{const header=document.querySelector('.site-header'),progress=document.querySelector('#progress'),toggle=document.querySelector('#menuToggle'),mobile=document.querySelector('#mobileNav'),toast=document.querySelector('#toast');const update=()=>{const total=document.documentElement.scrollHeight-window.innerHeight; if(progress)progress.style.width=`${total>0?(scrollY/total)*100:0}%`;header?.classList.toggle('scrolled',scrollY>20)};addEventListener('scroll',update,{passive:true});update();toggle?.addEventListener('click',()=>{const open=toggle.getAttribute('aria-expanded')==='true';toggle.setAttribute('aria-expanded',String(!open));mobile?.classList.toggle('open',!open)});mobile?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{toggle?.setAttribute('aria-expanded','false');mobile?.classList.remove('open')}));document.querySelectorAll('[data-year]').forEach(el=>el.textContent=new Date().getFullYear());const show=(message)=>{if(!toast)return;toast.textContent=message;toast.classList.add('show');clearTimeout(window.toastTimer);window.toastTimer=setTimeout(()=>toast.classList.remove('show'),3200)};document.querySelector('#contactForm')?.addEventListener('submit',e=>{e.preventDefault();show('Thanks — you are on the Nexa-hub early access list.');e.target.reset()});document.querySelectorAll('.prompt-send,.prompt-line').forEach(el=>el.addEventListener('click',()=>show('Nexa-hub is getting ready — your workspace is coming soon.')));document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',e=>{const target=document.querySelector(a.getAttribute('href'));if(target){e.preventDefault();target.scrollIntoView({behavior:'smooth'})}}));});
